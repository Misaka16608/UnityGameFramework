# CLAUDE.md

> **规则：本地 commit 必须经我确认后才能执行。确认后自动推送到远端仓库。**

## Project Overview

UnityGameFramework — GameFramework 的 Unity 集成层，提供 `UnityGameFramework.Runtime` 和 `.Editor` 两个程序集。

包含 submodule: `GameFramework`（纯 .NET 核心）。
